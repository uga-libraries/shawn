# Misc scripts written by Shawn

## ghost.py
Creates a ghost copy of a given directory. It replicates the file structure, but all the files are 0 byte files. Good for testing scripts before using on real data.
